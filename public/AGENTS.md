# 공개 자산 규칙

- 모든 파일은 GitHub Pages에서 그대로 공개된다고 가정한다.
- 시크릿, 내부 문서, 원본 개인정보, 라이선스가 불명확한 자산을 넣지 않는다.
- 벡터는 SVG, 래스터 이미지는 WebP를 우선한다.
- 파일명은 설명적인 kebab-case를 사용하고 사용하지 않는 자산은 남기지 않는다.
- 이미지 추가 시 의미 있는 alt text를 사용처에 함께 작성한다.
