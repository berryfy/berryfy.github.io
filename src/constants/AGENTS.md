# 전역 상수 규칙

- 사이트 전체에서 공유하는 값만 둔다. 기능 전용 값은 `src/features/<feature>/constants`에 둔다.
- 브랜드·사업자·도메인·라우트·지원 언어를 다른 파일에 중복 선언하지 않는다.
- 객체는 변경 불가능하게 선언하고 `as const` 또는 `satisfies`로 타입을 보존한다.
- 컴포넌트, React 상태, 계산 로직을 넣지 않는다.
- 공개 저장소이므로 토큰, API 키, 내부 주소, 실제 환경 변수는 금지한다.
